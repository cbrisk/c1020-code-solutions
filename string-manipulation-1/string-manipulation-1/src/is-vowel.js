/* eslint-disable no-unused-vars */
function isVowel(char) {
  var checker = char.toUpperCase();
  if (checker === 'A' || checker === 'E' || checker === 'I' || checker === 'O' || checker === 'U') {
    return true;
  } else {
    return false;
  }
}
